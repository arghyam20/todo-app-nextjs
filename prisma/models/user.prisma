model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String
  image     String?
  todos     Todo[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
